export enum Status {
  Active = 1,
  Inactive = 2,
  Deleted = 3,
}

export enum Role {
  Admin = 4,
  Driver = 5,
}

export enum TripStatus {
  Pending = 6,
  InProgress = 7,
  Completed = 8,
}

export enum TripUpdateStatus {
  Start = 9,
  Pause = 10,
  Resume = 11,
  End = 12,
}
